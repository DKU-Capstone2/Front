const CommisionList = ({ first, second, third }) => {
  return (
    <header className="CommisionList">
      <div>{first}</div>
      <div>{second}</div>
      <div>{third}</div>
    </header>
  );
};

export default CommisionList;
